{
  "name": "my-react-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^7.1.3",
    "@vitejs/plugin-react": "^4.3.2"
  }
}

{
  "scripts": {
    "build": "react-scripts build",  // For Create React App
    // OR
    "build": "npm run build",        // Alternative
    // OR if using Vite
    "build": "vite build"
  }
}
